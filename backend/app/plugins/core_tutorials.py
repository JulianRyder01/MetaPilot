"""官方核心（MetaPilot 文档库）自带的使用教程。

教程由核心自身维护（id/title/summary/content 全部为 markdown 文本），
经 /api/plugins 清单与各插件声明的 tutorials 合并下发，前端「使用教程」页统一渲染。
核心不感知任何插件的教程声明，二者在展示层聚合（解耦）。
"""

CORE_TUTORIALS: list[dict] = [
    {
        "id": "core-overview",
        "title": "平台概览：学、练、玩、库",
        "summary": "MetaPilot 是什么、四大模块（学习/练习/互动/知识库）与桌面端说明，快速建立整体认识。",
        "content": """# 平台概览

MetaPilot 是一个把「学、练、玩」结合起来的交互式学习平台，同时内置一套 Obsidian 风格的知识库（文档库）。整体打包为 Electron 桌面应用，可分发到 Windows / macOS / Linux。

## 四大模块

| 模块 | 作用 |
|---|---|
| **学** | 每个知识点是一个「组件流」页面：Markdown 讲解、选择题 / 填空题 / 简答题（主观题由 AI 判分）、动态交互块（独立 HTML，可做物理模拟与可视化） |
| **练** | 每章可标记学完，课程进度一目了然；学习进度独立缓存，随时回到上次学习的位置 |
| **玩** | 动态交互块让抽象的知识点变得可操作、可观察（如卷积可视化、频谱变换实验） |
| **库** | 基础结构是「库 → 文件夹 → 文档 → 小节」，课程只是其中一种文件夹类型；还支持导入 Markdown / Obsidian 笔记，并用 AI 洞察查阅资料联系、问答溯源并生成图表/课程 |

## 顶栏导航

- 左侧「我的库」：进入知识库首页；
- 中部为各插件贡献的导航（统计、AI 洞察等，随插件启用/禁用出现或消失）；
- 右侧「插件」「设置」：管理插件与系统配置；语言（简体中文 / 繁体中文 / English）与主题（明暗/特色主题）也在右侧随时切换。

## 快速开始

1. 启动后端与前端（开发模式见仓库 README「快速开始」；桌面版直接安装运行）；
2. 在「我的库」新建一个库（或导入课程包 / Markdown 笔记）；
3. 打开库，新建文档或课程，进入编辑开始填写内容；
4. 回到库页打开文档即可阅读 / 学习；「统计」查看使用数据；「插件」安装更多能力。
"""
    },
    {
        "id": "core-library",
        "title": "库与文档：管理你的知识",
        "summary": "库-文件夹-文档-小节 的层级结构、Markdown / Obsidian 导入、图表画布与数据目录（vault）说明。",
        "content": """# 库与文档

MetaPilot 的知识以「**库 → 文件夹 → 文档 → 小节**」四级结构组织，Obsidian 风格的文件树，一切可改。

## 创建库

在「我的库」首页点击「新建库」，输入名称即可。可设置置顶 / 默认库（AI 洞察等插件的默认保存目标）、重命名与删除（删除需二次确认）。

## 文件夹 / 文档 / 小节

- **文件夹**：库内的分类容器，类型有普通文档集（note）、知识库（kb）、课程（course，由交互式学习插件提供）、图表（canvas）。不同类型的打开路由由插件的 `collection_kinds` 声明，核心不写死。
- **文档**：文件夹下的内容单元，可含多篇文档；每篇文档有简介、作者、版本等元数据。
- **小节**：文档内的知识点，每个小节由若干「组件块」组成（Markdown、题目、交互块、引用等）。

支持的块类型由官方核心与各插件共同声明：Markdown 是核心内置；选择题/填空题/简答题/交互块由「交互式学习」插件提供，未启用该插件时以原始数据展示。

## 导入

- 库页「导入」对话框支持：**Markdown 笔记**（`.md`，核心能力）、**课程包**（`.mpf` 或 zip，由交互式学习插件提供 tab）、**图表画布**（`.canvas`，Obsidian 直接兼容）。
- 桌面端数据目录（vault）默认位于操作系统用户数据目录，可在「设置 → 数据目录」查看/迁移，卸载或升级不影响数据。

## 图表画布

「新建图表」创建 `.mpf` canvas 集合；画布编辑器支持节点拖拽/连线/多选/撤销重做/缩放平移，可导出原生 `.canvas` 文件（Obsidian 可直接打开）或 `.mpf`。
"""
    },
    {
        "id": "core-ai",
        "title": "AI 能力：网关、判题与洞察",
        "summary": "统一 AI 网关配置、主观题 AI 判分、AI 洞察（检索/问答/规划生成）与本地模型一键启停。",
        "content": """# AI 能力

MetaPilot 的 AI 能力统一走**核心 AI 网关**：密钥与地址由核心持有（存 `.env`，不上云），插件与页面经网关中转调用，自动记录调用次数 / token / 成本到「统计 → AI 用量」。

## 配置

「设置 → AI 服务」配置 API 服务商（OpenAI 兼容 / Anthropic / 本地模型）：填入 key、地址、模型与价格即可。内置本地模型（Qwen3-Embedding 0.6B/4B、Qwen3-4B、Qwen3-Reranker）可一键下载并启动，网络受限环境也能离线使用。

## 主观题 AI 判分

学习页的简答题由 AI 对比用户输入与参考答案，输出准确率与评语。判分模型复用已配置的大模型，无需额外设置。

## AI 洞察（插件）

「AI 洞察」插件对库/文件夹/文档与软链接挂载做多粒度向量化，提供四种思考模式：辅助思考、思维漫游、反思归纳、洞察规划。洞察规划可多轮推理生成图表或课程；问答支持溯源，标注答案来自哪篇文档。

## 动态交互 HTML

交互式学习插件的高级交互块（`interactive` 类型）支持 AI 生成文本、添加评判上下文、结束并提交 AI 评判，评判结果生成 Markdown/HTML 结果页保存在交互块内。多模态配置未开启时，提交图片会收到提示。
"""
    },
    {
        "id": "core-plugin",
        "title": "插件体系：安装、启用与商店",
        "summary": "插件分类（核心/官方/用户）、启用与禁用、插件商店安装与上传自制插件。",
        "content": """# 插件体系

MetaPilot 的功能以插件形式启用：官方核心（MetaPilot 本身，不可禁用）· 官方插件（官方开发，可禁用）· 用户自定义插件（可开发、禁用、删除）。

## 插件页面

「插件」导航进入插件管理页：按 用户自定义 → 官方插件 → 官方核心 分组展示，可查看每个插件的版本、来源、功能清单与更新历史；开关即启用/禁用，无需重启（后端路由门禁同步生效）。

## 插件商店

插件页「商店」Tab 可浏览、筛选并一键安装商店插件（安装后立即生效）。商店是独立部署的服务（plugins-store），主后端通过 `.env` 的 `PLUGIN_STORE_URL` 指向它；未配置时商店功能关闭。

## 上传与发布

插件页支持上传 zip 本地安装（插件包 = `plugin.json` + 后端 `__init__.py` + 可选前端 `frontend/frontend.js`），也可发布到商店供他人安装。开发规范见仓库 `docs/04-插件开发指南.md`。

## 插件使用教程

本页下方会按插件分组列出各插件自己的使用教程——由插件自身在 `plugin.json` 的 `tutorials` 字段声明，核心只做聚合展示，不写死任何插件内容。只显示**已启用**插件的教程，禁用后自动隐藏。
"""
    },
    {
        "id": "core-tips",
        "title": "日常使用技巧与常见问题",
        "summary": "学习进度、统计、快捷键与常见疑问解答，帮您把平台用得更顺手。",
        "content": """# 使用技巧与常见问题

## 学习与进度

- 学习页左栏为大纲导航，底部「上一个 / 下一个」切换知识点；滑到页面底部自动标记学完（也可手动标记/取消）。
- 学习进度按课程独立缓存：上次学习位置、每节完成状态随时可回；统计页按 今日/本周/本月/全部 汇总学习时长。
- 进度圆圈：完成显示绿色对勾，点击圆圈可二次确认清除该节进度。

## 编辑

- 编辑页一切可改：库、文件夹、章节、小节、题目与答案；组件流按块增删排序。
- 课程等补丁类型在文档结构上叠加插件能力（进度/判题/交互块），禁用插件时仍可编辑/阅读文档结构本身。

## 常见问题

**Q：页面提示「需要启用 XX 插件」？**
该功能由对应插件提供，前往「插件」页启用即可；第三方插件还能从商店一键安装。

**Q：AI 不工作？**
检查「设置 → AI 服务」是否已配置可用服务商（或已启动本地模型），并确认模型支持所需能力（如多模态）。

**Q：数据存在哪里？**
桌面版数据（vault）、`.env` 与可写插件目录都放在操作系统用户数据目录，卸载/升级不影响数据；开发版数据在仓库 `data/` 目录。

**Q：如何让 AI 快速理解某个页面？**
点击顶栏右侧「复制整页」按钮，可把当前页面一键复制为 **HTML** 或 **Markdown** 文本，直接粘贴给 AI 工具阅读。
"""
    },
]